# MCP (Model Context Protocol) Server Configuration
# Each server provides additional tools/resources to the agent

servers:
  # Example: Filesystem MCP server
  # - name: filesystem
  #   command: npx
  #   args: [-y, "@modelcontextprotocol/server-filesystem", "/Users/zayed"]

  # Example: GitHub MCP server
  # - name: github
  #   command: npx
  #   args: [-y, "@modelcontextprotocol/server-github"]
  #   env:
  #     GITHUB_PERSONAL_ACCESS_TOKEN: ${GITHUB_TOKEN}

  # Example: Brave Search MCP server
  # - name: brave-search
  #   command: npx
  #   args: [-y, "@modelcontextprotocol/server-brave-search"]
  #   env:
  #     BRAVE_API_KEY: ${BRAVE_API_KEY}

  # Example: Puppeteer MCP server (browser automation)
  # - name: puppeteer
  #   command: npx
  #   args: [-y, "@modelcontextprotocol/server-puppeteer"]

  # Example: SQLite MCP server
  # - name: sqlite
  #   command: npx
  #   args: [-y, "@modelcontextprotocol/server-sqlite", "--db-path", "./data.db"]

  # Example: Custom local MCP server
  # - name: my-server
  #   command: /path/to/my-mcp-server
  #   args: [--some-flag]
  #   cwd: /path/to/working/dir
